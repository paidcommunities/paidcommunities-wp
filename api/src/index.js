import {default as activate} from './activate';
import {default as deactivate} from './deactivate';

export {
    activate,
    deactivate
}

const api = {
    activate,
    deactivate
}

export default api;